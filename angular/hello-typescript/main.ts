var minhaVar = "minha variável";

function minhaFuncao(x,y){
    return x + y;
}

// ES6 - 2015
let num = 2;
const PI = 3.14;

var numeros = [1,2,3];
numeros.map(v => v * 2);

class Matematica {
    soma = (x, y) => x + y;
}

var str : string = 'string';
var n1 : any = 'qualquer tipo de variável';
